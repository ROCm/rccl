# rccl-UnitTestsFixtures — shared/ctest schema (ROCm 6.4+).
# RCCL does not support Windows: exclude_windows matches all tests so generated suites are empty on Windows.
#
# Tiers:
#   quick — smoke (curated patterns).
#   standard — wildcards per suite.
#   full — merges standard; override labels.
#   full — merge standard; override labels.

test_categories:
  quick:
    description: "Smoke — curated BitOps / Comm / EnqueueCount cases (fast, representative)."
    test_patterns:
      - "DIVUP.*"
      - "ROUNDUP.*"
      - "ALIGN_POWER.*"
      - "ALIGN_SIZE.*"
      - "u32fp8MaxValue.*"
      - "u32fp8Decode.*"
      - "u32fp8Encode.*"
      - "u32fpEncode.*"
      - "u32fpDecode.*"
      - "getHash.*"
      - "eatHash.*"
      - "BitOpsTemplateAllIntTestsFixture.*"
      - "BitOpsTemplateUnsignedTestsFixture.*"
      - "CommTests.Sorter"
      - "EnqueueCountTests.*"
    exclude_windows:
      - "*"
    labels:
      - "rccl"
      - "quick"
      - "smoke"
      - "precheck"
      - "fixtures"
    env_variables: &fixtures_env
      - "NCCL_DEBUG=INFO"
  standard: &fixtures_standard
    description: "Broad regression — every suite in this binary (wildcard per suite), bounded timeouts."
    test_patterns:
      - "CommTests.*"
      - "EnqueueCountTests.*"
      - "BitOpsTemplateAllIntTestsFixture.*"
      - "BitOpsTemplateUnsignedTestsFixture.*"
      - "DIVUP.*"
      - "ROUNDUP.*"
      - "ALIGN_POWER.*"
      - "ALIGN_SIZE.*"
      - "u32fp8MaxValue.*"
      - "u32fp8Decode.*"
      - "u32fp8Encode.*"
      - "u32fpEncode.*"
      - "u32fpDecode.*"
      - "getHash.*"
      - "eatHash.*"
    exclude_windows:
      - "*"
    labels:
      - "rccl"
      - "standard"
      - "regression"
      - "fixtures"
    env_variables: *fixtures_env
  comprehensive:
    <<: *fixtures_standard
    labels:
      - "rccl"
      - "comprehensive"
      - "comprehensive_regression"
      - "fixtures"
  full:
    <<: *fixtures_standard
    labels:
      - "rccl"
      - "full"
      - "full_regression"
      - "fixtures"

execution_settings:
  timeout_multiplier: 1
  category_timeouts:
    quick: 1200
    standard: 3600
    comprehensive: 3600
    full: 3600
