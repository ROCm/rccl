diff --git a/CMakeLists.txt b/CMakeLists.txt
index 712468d..9197847 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -22,6 +22,7 @@ option(MSCCLPP_USE_CUDA "Use NVIDIA/CUDA." OFF)
 option(MSCCLPP_USE_ROCM "Use AMD/ROCm." OFF)
 option(MSCCLPP_BYPASS_GPU_CHECK "Bypass GPU check." OFF)
 option(MSCCLPP_NPKIT_FLAGS "Enable NPKIT" OFF)
+option(CLIP_ENABLED "Enabling clip on add_elements" OFF)
 
 if(MSCCLPP_BYPASS_GPU_CHECK)
     if(MSCCLPP_USE_CUDA)
@@ -144,6 +145,9 @@ endif()
 if(MSCCLPP_NPKIT_FLAGS)
     target_compile_definitions(mscclpp_obj PRIVATE ${MSCCLPP_NPKIT_FLAGS})
 endif()
+if(CLIP_ENABLED)
+    target_compile_definitions(mscclpp_obj PUBLIC CLIP_ENABLED)
+endif()
 
 # libmscclpp
 add_library(mscclpp SHARED)
diff --git a/apps/nccl/src/allreduce.hpp b/apps/nccl/src/allreduce.hpp
index a14dfbc..3d0604c 100644
--- a/apps/nccl/src/allreduce.hpp
+++ b/apps/nccl/src/allreduce.hpp
@@ -70,17 +70,29 @@ __forceinline__ __device__ __bfloat162 clip(__bfloat162 val) {
 
 template <typename T>
 __forceinline__ __device__ T add_elements(T a, T b) {
-  return clip(a + b);
+  #ifdef CLIP_ENABLED
+   return clip(a + b);
+  #else
+   return a + b;
+  #endif
 }
 
 template <>
 __forceinline__ __device__ __half2 add_elements(__half2 a, __half2 b) {
-  return clip(__hadd2(a, b));
+  #ifdef CLIP_ENABLED
+   return clip(__hadd2(a, b));
+  #else
+    return __hadd2(a, b);
+  #endif
 }
 
 template <>
 __forceinline__ __device__ __bfloat162 add_elements(__bfloat162 a, __bfloat162 b) {
-  return clip(__hadd2(a, b));
+  #ifdef CLIP_ENABLED
+    return clip(__hadd2(a, b));
+  #else
+    return __hadd2(a, b);
+  #endif
 }
 
 template <typename T>
@@ -360,7 +372,7 @@ __global__ void __launch_bounds__(1024, 1)
 }
 
 template <typename T>
-__global__ void __launch_bounds__(512, 1)
+__global__ void __launch_bounds__(1024, 1)
     allreduce8(T* buff, T* scratch, T* resultBuff, mscclpp::DeviceHandle<mscclpp::SmChannel>* smChannels,
                mscclpp::DeviceHandle<mscclpp::SmChannel>* smOutChannels, size_t channelOutDataOffset,
                size_t channelScratchOffset, int rank, int nRanksPerNode, int worldSize, size_t nelems) {
@@ -851,7 +863,7 @@ cudaError_t allreduce(T* buff, T* scratch, T* resultBuff, mscclpp::DeviceHandle<
 #endif
   } else {
     int nBlocks = 8 * (nRanksPerNode - 1);
-    int nThreadsPerBlock = 512;
+    int nThreadsPerBlock = 1024;
     if (hieAllred && worldSize >= 8) {
 	nBlocks = 20;
 	allreduce10<<<nBlocks, nThreadsPerBlock, 0, stream>>>(buff, scratch, resultBuff, smChannels, smScrChannels,
